name: Branch name

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  branch-name:
    uses: group10-tc-01/fcs-pipelines/.github/workflows/branch-name-check.yml@main
